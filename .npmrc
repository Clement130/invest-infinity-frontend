# Configuration npm locale pour ce projet
# Ce fichier évite le problème d'installation des devDependencies
# causé par la configuration globale omit=dev

# Note: omit="" n'est pas valide, on ne définit pas omit du tout
# pour que npm utilise le comportement par défaut (inclure toutes les dépendances)

