# Permanent Tasks for Cursor

- Create and maintain Supabase migrations (tables, relations, indices).
- Implement Supabase Auth (client + admin).
- Implement RBAC (client/admin) with full RLS policies.
- Create course/module/lesson system.
- Integrate Bunny Stream secure video embeds.
- Add Stripe payments + webhook verification.
- Create client dashboard (courses, progression).
- Create admin dashboard (CRUD courses, lessons, users).
- Maintain clean folder structure inside src/.
- Optimize code quality, performance and security continuously.
